# Cadena de conexión a PostgreSQL. Para desarrollo local puedes usar un
# Postgres local, o (más simple) una base gratis en https://neon.tech.
# Ejemplo con Neon (usa el connection string "pooled" que te da su panel):
DATABASE_URL="postgresql://usuario:password@host/basededatos?sslmode=require"
